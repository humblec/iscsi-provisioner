kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: iscsivolume
  annotations:
    volume.beta.kubernetes.io/storage-class: "hchiramm"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
